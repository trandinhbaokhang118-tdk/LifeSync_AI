# Disable the progress spinner and fancy output. On some Windows/PowerShell
# setups the animated spinner garbles piped output and can appear to "hang".
progress=false
fund=false
audit=false
loglevel=warn
